export enum LadderObjectType {
  BRANCH = 'BRANCH',
  CIRCUIT = 'CIRCUIT',
  INSTRUCTION = 'INSTRUCTION',
  RUNG = 'RUNG',
}

export enum RoutineType {
  LD = 'ld',
  ST = 'st',
}

export enum RoutineAssignment {
  NONE = 'NONE',
  MAIN = 'MAIN',
}
